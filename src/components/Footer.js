import React from 'react';
import Nav from './Nav';

function Footer(props) {

    return (
        <footer className="normal-footer">
            <h1>{props.title}</h1>
            hi
        </footer>
    );
}
export default Footer;